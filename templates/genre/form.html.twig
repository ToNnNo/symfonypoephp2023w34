{% extends 'base.html.twig' %}

{% block title %}Editer un genre{% endblock %}

{% block body %}
    <div class="mb-3">
        <a href="{{ path('genre_index') }}" class="btn btn-outline-dark btn-sm">Revenir à la liste des genres</a>
    </div>

    {{ form_start(genreForm) }}

    {{ form_rest(genreForm) }}

    <button class="btn btn-outline-primary btn-sm">Enregistrer</button>
    {{ form_end(genreForm) }}

{% endblock %}
